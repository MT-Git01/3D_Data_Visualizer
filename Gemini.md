AntigravityプラットフォームとPythonを用いた、CSVデータから3Dグラフを描画するアプリケーションの仕様書（`Gemini.md`）を作成しました。

この仕様書は、AIエージェントや開発ツール（Claude Code等）がそのまま解釈してコードを生成できるように、StreamlitとPlotlyをベースにした具体的な実装要件として構造化しています。

---

# Gemini.md

## 1. 概要

本アプリケーションは、アップロードされたCSVファイルから3Dデータを読み込み、多様な3Dプロット（点群、線、スプライン、コンター、サーフェス）をインタラクティブに生成・操作・エクスポートするためのAntigravity用Webアプリケーションである。

---

## 2. システム要件 & 技術スタック

* **ランタイム環境:** Python 3.10+
* **主要ライブラリ:**
* `streamlit`: UI構築、ファイルアップロード、ユーザーインタラクションの制御
* `pandas`: CSVデータの読み込み、データフレーム処理
* `numpy`: 数値計算（特にスプライン補間、サーフェス生成用）
* `plotly`: インタラクティブな3Dグラフ描画、回転、画像エクスポート
* `scipy`: 3Dスプライン曲線補間、および散布図からの不規則格子サーフェス（GridData）生成用



---

## 3. 機能要件

### 3.1. データ入力 (CSVインポート)

* **UI要素:** ファイルアップローダー（`st.file_uploader`）
* **仕様:**
* 拡張子 `.csv` のみ受け付ける。
* **ヘッダー判定:** 1行目が数値でない（文字列である）場合は、自動的に軸のラベル（X軸、Y軸、Z軸、およびカラー値）として採用する。ヘッダーがない（1行目から数値の）場合は、デフォルトで `X`, `Y`, `Z`, `Value` をラベルとする。


* **列数に応じたデータハンドリング:**
* **3列の場合:** 1列目=$\text{X}$座標, 2列目=$\text{Y}$座標, 3列目=$\text{Z}$座標。
* **4列の場合:** 1列目=$\text{X}$座標, 2列目=$\text{Y}$座標, 3列目=$\text{Z}$座標, 4列目=$\text{スカラー値（C）}$。



### 3.2. グラフ表示モード (5つの選択肢)

ユーザーはサイドバーまたはメイン画面のラジオボタン等で以下のモードを切り替えられる。

1. **点群 (3D Scatter):** $\text{X, Y, Z}$ 座標に点をプロット。
2. **点と点の間を線で繋ぐ (3D Line):** CSVの行順（上から下）に従い、各点を直線で結ぶ。
3. **点と点の間をスプライン曲線で繋ぐ (3D Spline Line):** `scipy.interpolate` 等を用いて、行順のデータ点間を滑らかな3Dスプライン曲線で補間して描画する。
4. **点群表示＋カラー（コンタープロット）:** 4列目のデータがある場合のみ有効。スカラー値の大小に応じて、各点の色を変化させる。
5. **サーフェス表示＋コンター（3D Surface Contour）:** 4列目のデータがある場合のみ有効。点群をベースに空間上の表面（メッシュ）を生成し、その表面上に4列目の値に応じたグラデーション（コンター）を表示する（※不規則点データの場合は `scipy.interpolate.griddata` を用いてグリッド化を行う）。

### 3.3. UIコントロール & パラメータ調整

サイドバーに以下の調整用コントロールを配置する。

| コントロール名 | UIタイプ | 設定範囲 / 仕様 | 対象モード |
| --- | --- | --- | --- |
| **表示モード選択** | ラジオボタン/セレクトボックス | 上記5モードの選択 | 全体 |
| **点サイズ** | スライダー | 1 ～ 20 (デフォルト: 5) | 点群ベースのモード |
| **軸ラベルサイズ** | スライダー | 10 ～ 30 (デフォルト: 14) | 全体 |
| **コンターレンジ (最小/最大)** | ダブルヘッドスライダー | スカラー値の [Min, Max] を初期値とし、任意に調整可能 | 4列目データ使用時 |

### 3.4. グラフ操作 & レンダリング

* Plotlyの3Dエンジン（`go.Scatter3d`, `go.Surface` 等）を採用。
* **インタラクション:** マウスのドラッグによる3D回転、ホイールによる拡大・縮小、パン（平行移動）を標準サポート。
* **コンターバー（カラーバー）:** 4列目の値を使用するモードでは、選択されたコンターレンジに応じたカラーバーをグラフ右側に常時表示し、色と数値のスケールを明示する。

### 3.5. グラフエクスポート

* 表示されているグラフを以下のフォーマットでダウンロードできるボタンを設置する。
* **PNG / JPEG:** 静止画エクスポート
* **PDF:** 印刷・レポート用エクスポート


* Plotlyの `to_image` またはベクター書き出し機能を利用し、ローカルにダウンロードさせる。

---

## 4. 画面レイアウト案 (UI/UX)

```
+-----------------------------------------------------------------------+
|  [Title] 3D Data Visualizer for Antigravity                           |
+-----------------------------------------------------------------------+
| [Sidebar]                         | [Main Panel]                      |
|                                   |  +-----------------------------+  |
| 1. Data Upload                    |  |                             |  |
|    [ Browse files (CSV) ]         |  |                             |  |
|                                   |  |        3D Graph Area        |  |
| 2. Plot Settings                  |  |    (Rotatable / Zoomable)   |  |
|    Mode: (o) Point Cloud ( ) Line |  |                             |  |
|                                   |  +-----------------------------+  |
| 3. Style Controls                 |  | [Export Buttons]            |  |
|    - Point Size:  [---o---]       |  |  [PNG]  [JPEG]  [PDF]       |  |
|    - Font Size:   [--o----]       |  +-----------------------------+  |
|                                   |  | [Data Preview Table]        |  |
| 4. Contour Range (4-col only)     |  |                             |  |
|    [Min ======o====== Max]        |  +-----------------------------+  |
+-----------------------------------------------------------------------+

```

---

## 5. 実装時の注意点（エージェントへの指示）

* **エラーハンドリング:** * 4列必要なモード（コンター、サーフェス）が選択された状態で3列のCSVがアップロードされた場合、「4列のデータを含むCSVをアップロードしてください」と警告（`st.warning`）を出すこと。
* CSVのデータ行数がスプライン補間に足りない場合（例: 3点未満など）の例外処理を入れること。


* **パフォーマンス:** 点群の数が極端に多い場合（数万点以上）の描画遅延を避けるため、Plotlyの `webgl` ベースの描画が維持されるよう実装すること。